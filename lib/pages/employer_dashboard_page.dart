import 'package:flutter/material.dart';

class EmployerDashboardPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Employer Dashboard'),
      ),
      body: Center(
        child: Text('Employer Dashboard Page'),
      ),
    );
  }
}